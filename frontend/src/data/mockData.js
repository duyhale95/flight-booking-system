import airports from '../mocks/data/airports.json';
import airlines from '../mocks/data/airlines.json';
import routes from '../mocks/data/routes.json';
import flightSchedules from '../mocks/data/flight_schedules.json';

export const mockData = {
  airports,
  airlines,
  routes,
  flightSchedules
};

export default mockData; 